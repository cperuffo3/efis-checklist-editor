import {
  saveWorkspace,
  loadWorkspace,
  saveWindowState,
  loadWindowState,
  savePanelState,
  loadPanelState,
} from "./handlers";

export const persistence = {
  saveWorkspace,
  loadWorkspace,
  saveWindowState,
  loadWindowState,
  savePanelState,
  loadPanelState,
};
